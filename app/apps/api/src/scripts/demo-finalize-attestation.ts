/**
 * Bootstrap finalize (coordinator as vault) is retired.
 * Use demo-squads-finalize.ts so evidence is a real Squads 2-of-3 execute.
 */
console.error(
  "demo-finalize-attestation.ts is retired. Run demo-squads-finalize.ts instead so the committee vault is the Squads 2-of-3 PDA, not the coordinator."
);
process.exit(1);
