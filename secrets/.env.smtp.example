# Local SMTP for docker compose (gitignored as secrets/.env*).
# Copy to secrets/.env.smtp and fill real values. Never commit the filled file.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_SECURE=false
